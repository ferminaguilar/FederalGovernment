{
  "name": "Drupal Environment",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "forwardPorts": [8080],
  "postCreateCommand": "composer install && chmod +x setup.sh && ./setup.sh && ln -sf /workspaces/FederalGovernment/vendor/bin/drush /usr/local/bin/drush && chown vscode:vscode -R /workspaces/FederalGovernment"
  }
